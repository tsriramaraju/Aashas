name: Auth service actions

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: cd server/auth && npm i && npm run test:ci
        env:
          CI: true
          EXCHANGE_OMS_ID: 1
          ISOLATE: true
