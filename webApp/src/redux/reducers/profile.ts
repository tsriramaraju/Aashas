const initialState = {
  profile: {},
};

const profile = (state = initialState, action: any) => {
  const { type, payload } = action;
  switch (type) {
    default:
      return state;
  }
};

export { profile };
